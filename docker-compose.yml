version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      - DB_HOST=host.docker.internal
      - DB_USER=admin
      - DB_PASSWORD=admin#321
      - DB_NAME=mydb
